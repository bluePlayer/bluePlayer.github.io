/**
 * @author vlado
 */

document.onkeydown = function (e) {
    'use strict';
  var kc = e.keyCode,
        key = e.key;

  //console.log("key code: " + kc + ", key: " + e.key);
  //switch (kc) {
    /*case 32:
      SI.GameScreen.keypressHandler(key, kc);
      return false;
    case 39:
      SI.GameScreen.keypressHandler(key, kc);
      return false;
    case 45:
      SI.GameScreen.keypressHandler(key, kc);
      return false;
    case 46:
      SI.GameScreen.keypressHandler(key, kc);
      return false;
    case 222:
      SI.GameScreen.keypressHandler("'", 39);
      return false;
    case 126:
      SI.GameScreen.keypressHandler(key, kc);
      return false;
    case 191:
      SI.GameScreen.keypressHandler(key, kc);
      return false;
      */
  //}
};
